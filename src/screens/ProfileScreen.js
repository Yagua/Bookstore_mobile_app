import { Text, View } from 'react-native'

const ProfileScreen = () => {
    return (
        <View style={{alignItems: "center", justifyContent: "center", height: "100%"}}>
            <Text style={{fontSize: 20, marginBottom: 10}}>Profile Screen</Text>
        </View>
    );
}

export default ProfileScreen;
